version: "3"

services:
  amboss-event:
    container_name: amboss-event
    image: "amboss/amboss-event:latest"
    restart: unless-stopped
    environment:
      NODE_ENV: "production"
    depends_on:
      - postgres
      - redis

  amboss-server:
    container_name: amboss-server
    image: "amboss/amboss-server:latest"
    restart: unless-stopped
    environment:
      POSTGRES_URL: "postgres"
      NODE_ENV: "production"
      AMBOSS_HOST: ${AMBOSS_PROTOCOL:-https}://${AMBOSS_HOST}
      REDIS_HOST: "redis"
      POSTGRES_HOST: "postgres"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "4000:4000"
    depends_on:
      - postgres
      - redis

  amboss-web:
    container_name: amboss-web
    image: "amboss/amboss-web:latest"
    restart: unless-stopped
    environment:
      NODE_ENV: "production"
      SERVER_SSR_URL: "http://amboss-server:4000/graphql"
      SERVER_PATH: "/graphql"
    ports:
      - "3000:3000"
    depends_on:
      - amboss-server

required:
  - "postgres"
  - "redis"
  - "amboss"
recommended:
  - "opt-add-tor"
